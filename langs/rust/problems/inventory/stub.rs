// create_item(sku, name)
// stock(sku, delta)
// get_qty(sku)
// list_low(threshold)
// reserve(sku, n)
// release(sku, n)
// ship(sku, n)
pub struct Simulation;

impl Simulation {
    pub fn new() -> Self {
        Self
    }
}
