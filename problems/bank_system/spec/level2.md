# Bank system level 2

- `top_spenders(timestamp, n)` returns up to n account ids formatted `id(outgoing)` sorted by outgoing descending, then id ascending. Transfers out and payments count as outgoing.
