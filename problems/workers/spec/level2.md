# Workers level 2

- `top_n_workers(n, position)` returns `"id(time), ..."` for workers whose current position matches. Time is finished time in that position. Sort by time descending, then id ascending. Empty string if none.
